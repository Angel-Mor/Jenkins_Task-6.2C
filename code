pipeline {
    agent any
    
    tools {
        maven 'Maven 3.8.5' // Make sure this name matches your Jenkins Maven tool config
    }

    environment {
        SONARQUBE_ENV = 'sonar-server' // Jenkins SonarQube config name
        NOTIFY_EMAIL = 'angel4761.be23@chitkara.edu.in'
    }

    stages {

        stage('Build') {
            steps {
                echo 'Building the application...'
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'Build successful!'
                }
                failure {
                    echo 'Build failed.'
                    emailext(
                        subject: 'Build Failed',
                        body: 'Build stage failed. Please review Jenkins logs.',
                        to: "${env.NOTIFY_EMAIL}"
                    )
                }
            }
        }

        stage('Unit and Integration Tests') {
            steps {
                echo 'Running tests (JUnit/TestNG)...'
                sh 'mvn test'
            }
            post {
                always {
                    junit '**/target/surefire-reports/*.xml'
                    emailext(
                        subject: 'Test Results',
                        body: "Tests completed. Result: ${currentBuild.currentResult}",
                        to: "${env.NOTIFY_EMAIL}",
                        attachmentsPattern: '**/target/surefire-reports/*.xml'
                    )
                }
            }
        }

        stage('Code Analysis') {
            steps {
                echo 'Running static code analysis...'
                withSonarQubeEnv("${env.SONARQUBE_ENV}") {
                    sh 'mvn sonar:sonar'
                }
            }
            post {
                failure {
                    emailext(
                        subject: 'Code Analysis Failed',
                        body: 'SonarQube scan failed. Please check the logs.',
                        to: "${env.NOTIFY_EMAIL}"
                    )
                }
            }
        }

        stage('Security Scan') {
            steps {
                echo 'Running OWASP security scan...'
                dependencyCheck additionalArguments: '--scan ./ --format XML --out ./', odcInstallation: 'OWASP-DC'
                dependencyCheckPublisher pattern: 'dependency-check-report.xml'
            }
            post {
                always {
                    emailext(
                        subject: 'Security Scan Completed',
                        body: "Security scan result: ${currentBuild.currentResult}",
                        to: "${env.NOTIFY_EMAIL}",
                        attachmentsPattern: 'dependency-check-report.xml'
                    )
                }
            }
        }

        stage('Deploy to Staging') {
            steps {
                echo 'Deploying to staging server...'
                sh '''
                    aws s3 cp target/myapp.jar s3://my-staging-bucket/
                    # Optionally trigger deployment scripts here
                '''
            }
        }

        stage('Integration Tests on Staging') {
            steps {
                echo 'Running staging integration tests...'
                sh 'mvn verify -Denvironment=staging'
            }
        }

        stage('Deploy to Production') {
            steps {
                echo 'Deploying to production server...'
                sh '''
                    aws s3 cp target/myapp.jar s3://my-production-bucket/
                    # Optionally add deployment triggers
                '''
            }
        }
    }

    post {
        always {
            echo 'Sending final pipeline status email...'
            emailext(
                subject: "Pipeline Completed: ${env.JOB_NAME} [#${env.BUILD_NUMBER}]",
                body: "Full details at: ${https://github.com/Angel-Mor/Jenkins_Task-6.2C}",
                to: "${agel4761.be23@chitkara.rdu.in}"
            )
        }
    }
}
